server:
  port: 801

spring:
  # 数据源
  datasource:
    # 数据库连接信息
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.3.3/wrench?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true
    username: root
    password: root
    initialization-mode: always

logging:
  level:
    root: info